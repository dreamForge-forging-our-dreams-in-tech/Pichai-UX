let image = String(document.body.style.backgroundImage);
image = image.substring(3, image.length - 1);

class PichaiUX {
    construct(options) {
        options = {
            source:  ?? '#008dcd',
        }
    }
}